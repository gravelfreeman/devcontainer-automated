# devcontainer-automated
Automated initialization of Colima-based devcontainers on macOS with 1Password SSH agent support. Open your project directly in VS Code inside the devcontainer and drop your favorite terminal into the same container without losing access to the host SSH agent.
